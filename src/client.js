import fetch from 'node-fetch';
import chalk from 'chalk';

const client = mozaik => {

	const apiCalls = {

		lastCommits ( params ) {

			/*To fill*/
		}
		
	}

	return apiCalls;

};

export default client;
